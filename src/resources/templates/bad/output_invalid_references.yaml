AWSTemplateFormatVersion: "2010-09-09"
Resources:
  Bucket:
    Type: AWS::S3::Bucket
Outputs:
  InvalidAttribute:
    Value: !GetAtt Bucket.NotARealAttribute
  UnresolvedSubVariable:
    Value: !Sub "prefix-${MissingTarget}"
