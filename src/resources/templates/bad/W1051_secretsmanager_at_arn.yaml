AWSTemplateFormatVersion: "2010-09-09"
Resources:
  MyConnection:
    Type: AWS::DMS::Endpoint
    Properties:
      EndpointType: source
      EngineName: mysql
      SecretsManagerSecretId: "{{resolve:secretsmanager:my-secret}}"
