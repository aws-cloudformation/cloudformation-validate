AWSTemplateFormatVersion: "2010-09-09"
Description: Template with malformed core-schema tagged scalars.

Resources:
  Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !!int not_a_number
