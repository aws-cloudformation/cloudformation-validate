---
AWSTemplateFormatVersion: "2010-09-09"
Description: >
  E3510 valid: intrinsic-generated Resource ARNs must not false-positive
Parameters:
  BucketName:
    Type: String
    Default: my-bucket
Resources:
  PolicyWithJoinResource:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: test
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action: "s3:GetObject"
            Resource:
              Fn::Join:
                - ""
                - - "arn:aws:s3:::"
                  - !Ref BucketName
                  - "/*"
  PolicyWithSubResource:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action: "s3:*"
            Resource: !Sub "arn:aws:s3:::${BucketName}/*"
  PolicyWithRefResource:
    Type: AWS::IAM::UserPolicy
    Properties:
      UserName: test-user
      PolicyName: test
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action: "dynamodb:*"
            Resource: !Ref BucketName
  RoleInlinePolicy:
    Type: AWS::IAM::Role
    Properties:
      RoleName: test-role
      AssumeRolePolicyDocument:
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: "sts:AssumeRole"
      Policies:
        - PolicyName: inline
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - "logs:CreateLogGroup"
                  - "logs:CreateLogStream"
                Resource:
                  Fn::Sub: "arn:aws:logs:${AWS::Region}:${AWS::AccountId}:*"
  SSOPermissionSet:
    Type: AWS::SSO::PermissionSet
    Properties:
      InstanceArn: "arn:aws:sso:::instance/ssoins-12345"
      Name: ValidPermSet
      InlinePolicy:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowS3
            Effect: Allow
            Action: "s3:*"
            Resource: "*"
