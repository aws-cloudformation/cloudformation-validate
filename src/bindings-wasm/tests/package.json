{
    "private": true,
    "scripts": {
        "test": "vitest run",
        "test:coverage": "vitest run --coverage"
    },
    "dependencies": {
        "@aws/cloudformation-validate": "file:../dist"
    },
    "devDependencies": {
        "@types/node": "26.1.0",
        "@vitest/coverage-v8": "4.1.8",
        "vitest": "4.1.9"
    }
}
