name: Main CI

on:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build-and-test:
    uses: ./.github/workflows/validate.yml
    permissions:
      contents: read
      pull-requests: read
    with:
      ref: ${{ github.sha }}
      release-run: false
